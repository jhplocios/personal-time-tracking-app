export interface IActivityData {
  id: number;
  duration: number;
  tag: string;
  activityName: string;
}

export interface IActivityInput {
  duration: number;
  tag: string;
  activityName: string;
}